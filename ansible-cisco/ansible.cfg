[defaults]
host_key_checking = False
stdout_callback = json
timeout = 60
command_warnings = False

[paramiko_connection]
# Disable SSH key lookups - force password authentication only
# This prevents keyboard-interactive auth failures
look_for_keys = False
host_key_auto_add = True

[persistent_connection]
connect_timeout = 60
command_timeout = 60